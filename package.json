{
	"name": "uberschrift",
	"version": "1.0.0",
	"description": "Magically accessible and SEO friendly headings (`h1` — `h6`) for react",
	"keywords": [],
	"author": "developers@peerigon.com",
	"license": "Unlicense",
	"scripts": {
		"build:core": "pnpm --filter core build",
		"build:docs": "pnpm --filter docs build",
		"build:react": "pnpm --filter react build",
		"build:packages": "pnpm --filter \"./packages/**\" -r build",
		"lint": "pnpm -r lint",
		"test": "pnpm -r test",
		"watch:core": "pnpm --filter core watch",
		"watch:react": "pnpm --filter react watch",
		"watch": "pnpm --filter \"./packages/**\" -r watch"
	},
	"dependencies": {
		"@types/node": "18.11.10",
		"tsup": "^6.6.0"
	}
}
