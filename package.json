{
	"name": "uberschrift",
	"version": "1.0.0",
	"description": "Magically accessible and SEO friendly headings (`h1` — `h6`) for react",
	"keywords": [],
	"author": "developers@peerigon.com",
	"license": "Unlicense",
	"scripts": {
		"build:core": "pnpm --filter core build",
		"build:docs": "pnpm --filter docs build",
		"build:react": "pnpm --filter react build",
		"build:packages": "run-s build:core build:react",
		"lint:root": "eslint --max-warnings 0 --cache --ext js,jsx,cjs,mjs,ts,tsx --ignore-path .gitignore .",
		"lint": "pnpm -r lint",
		"test": "pnpm -r test",
		"watch:core": "./watch core",
		"watch:react": "./watch react",
		"watch": "run-p watch:core watch:react"
	},
	"dependencies": {
		"chokidar-cli": "^3.0.0",
		"esbuild": "^0.15.16",
		"esbuild-node-externals": "^1.5.0",
		"npm-run-all": "^4.1.5",
		"@types/node": "18.11.10"
	}
}
